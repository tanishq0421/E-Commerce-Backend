service: user-service
frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs20.x

functions:
  signup:
    handler: app/handler.Signup
    events:
      - httpApi:
          path: /Signup
          method: get

plugins:
  - serverless-offline
  - serverless-plugin-typescript
